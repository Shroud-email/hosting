{
  # Global options
  storage permissive_file_storage {
    root "/data"
  }

  email {$ADMIN_EMAIL}
}

{$APP_DOMAIN} {
  reverse_proxy web:8080
  tls {
    dns bunny {env.BUNNY_API_KEY}
  }
}

{$EMAIL_DOMAIN} {
  tls {
    dns bunny {env.BUNNY_API_KEY}
  }
}
